namespace Defines
{
    public enum EVoiceType
    {
        None,
        Always,
        PushToTalk,
        MaxCount,
    }

    public enum ESoundType
    {
        Bgm,
        Effect,
        MaxCount, 
    }
}